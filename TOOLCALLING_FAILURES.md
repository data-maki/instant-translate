# Toolcalling Failures

- 2026-05-19: `web.run` open with multiple search-result refs returned no visible rendered result. Retried with direct source URLs and the research succeeded.
- 2026-05-19: `python3 -m tomllib /Users/jcarbs/.codex/config.toml` failed because `tomllib` has no module entrypoint. Retried with `python3 -c 'import tomllib; ...'` and TOML validation succeeded.
- 2026-05-19: `python -m pytest backend/tests/test_app.py` failed with `zsh:1: command not found: python`; `python3.12 -m pytest backend/tests/test_app.py` then failed with `No module named pytest`. Task succeeded by using the repo virtualenv: `venv/bin/python -m pytest backend/tests/test_app.py`.
- 2026-05-19: `venv/bin/python - <<'PY' ... from app.main import app` failed with `ModuleNotFoundError: No module named 'app'` because the ad hoc backend probe did not include `PYTHONPATH=backend`. Task succeeded by rerunning with `PYTHONPATH=backend`.
- 2026-05-19: `PYTHONPATH=backend venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000` failed with `[Errno 48] address already in use`. Task succeeded by using the already-running backend on port 8000 and verifying `/health` plus `/context/places`.
- 2026-05-19: `mcp__chrome_devtools__.new_page` and `mcp__chrome_devtools__.list_pages` failed because the Chrome DevTools MCP profile was already running. Browser verification could not use Chrome DevTools in this turn.
- 2026-05-19: A live `/context/rewrite` sweep over all Japanese tone presets failed partway through with `requests.exceptions.ReadTimeout: HTTPSConnectionPool(host='api.groq.com', port=443): Read timed out`. Task continued by adding Groq timeout handling and rerunning the remaining cases.
- 2026-05-19: A stdin Python Groq sweep using `load_dotenv()` failed with `AssertionError` in `python-dotenv` `find_dotenv()`. Task continued by rerunning with `load_dotenv(dotenv_path=".env")`.
- 2026-05-19: `venv/bin/python -m pytest backend/tests/test_app.py` failed because a new test asserted exact hard-constraint list membership without the period in `Do not output Japanese.`. Task continued by changing the assertion to search within each constraint string.
- 2026-05-19: A live Groq+DeepL smoke test failed with `DeepL translation failed: 403 {"message":"Wrong endpoint. Use https://api-free.deepl.com..."}` because the local key is a DeepL Free key. Task continued by auto-selecting `https://api-free.deepl.com/v2/translate` for keys ending in `:fx`, with `DEEPL_API_URL` override support.
- 2026-05-19: A live Groq+DeepL smoke test after adding DeepL formality still failed with `DeepL translation failed: 403 {"message":"Forbidden..."}`. Local `.env` still contains a DeepL Free key shape ending in `:fx`, so the Pro upgrade is not visible to this process yet.
