2026-05-14 LEARNING: mother_in_law_decoder repo is at /Users/jcarbs/Code/mother_in_law_decoder and currently has a minimal README plus live_transcriber implementation; research notes can live at repo root RESEARCH_NOTES.md.
2026-05-19 LEARNING: Current live Soniox config in backend/app/soniox.py sends context only as {"text": context}; the async path in cli/live_transcriber/async_diarize.py sends context as a string. Soniox now documents structured context sections: general, text, terms, and translation_terms, with an 8,000-token limit.
2026-05-19 LEARNING: frontend/src/components/TranslatorApp.tsx uses BASE_CONTEXT as the textarea value, not only as placeholder copy, so the instructional text can be sent to Soniox if the user does not replace it.
2026-05-19 LEARNING: Codex MCP servers are configured in /Users/jcarbs/.codex/config.toml; removing Docker MCP and Granola MCP means deleting the [mcp_servers.MCP_DOCKER] and [mcp_servers.granola] tables there, not changing this app repo.
2026-05-19 LEARNING: Google Places enrichment in this repo uses GOOGLE_MAPS_API_KEY as the canonical server-side key name. Do not use GOOGLE_MAPS_SERVER_KEY or a fallback alias unless the user asks for a rename.
2026-05-19 LEARNING: Soniox realtime `stt-rt-v4` accepts structured context as `general: [{key,value}]`, `terms: string[]`, `text: string`, and `translation_terms: [{source,target}]`; this repo now sends that structured shape from the web start payload instead of bracketed pseudo-sections in one text blob.
2026-05-19 LEARNING: The app now keeps Soniox recognition/glossary context separate from Groq/Qwen social rewrite context. Live English utterances can be adapted through `/context/rewrite` with `GROQ_REWRITE_MODEL` defaulting to `qwen/qwen3-32b` and `reasoning_effort: none`.
2026-05-19 LEARNING: Local .env has SONIOX_API_KEY, GROQ_API_KEY, DEEPL_API_KEY, DEEPGRAM_API_KEY, OPENAI_API_KEY, and GOOGLE_MAPS_API_KEY present. The app expects a DeepL Pro API key for live upgrades and uses the Pro endpoint by default.
